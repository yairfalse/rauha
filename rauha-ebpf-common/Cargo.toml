[package]
name = "rauha-ebpf-common"
version = "0.1.0"
edition = "2021"
license = "Apache-2.0"

[features]
default = []
# Enable userspace-only helpers (requires std).
userspace = []

[dependencies]
# No dependencies — this crate must compile for bpfel-unknown-none.
