mod container;
mod io;
mod state;

fn main() {
    eprintln!("rauha-shim: stub — Phase 3 (Container Runtime) will implement this");
    eprintln!("One shim per zone. Communicates with rauhad via Unix socket + postcard.");
}
